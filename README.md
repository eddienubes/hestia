# hestia

hestia is an MCP server that searches Airbnb accommodations via natural language — discovery only, no booking/payment; click through the listing URL to book on airbnb.com yourself.

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run src/main.ts
```
